export type SensoryKind='AUDIO'|'VIDEO'|'MULTIMODAL';export interface SensoryObservation{kind:SensoryKind;input_ref:string;observation_ref:string;evidence_refs:string[];observed_at:string}export function validateSensoryObservation(x:SensoryObservation){return{ok:['AUDIO','VIDEO','MULTIMODAL'].includes(x.kind)&&x.input_ref.trim()!==''&&x.observation_ref.trim()!==''&&Array.isArray(x.evidence_refs)&&x.evidence_refs.length>0&&!Number.isNaN(Date.parse(x.observed_at))}}
