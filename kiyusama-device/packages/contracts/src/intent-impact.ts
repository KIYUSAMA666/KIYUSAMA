export interface IntentImpact{intent:string;impact:'LOW'|'MEDIUM'|'HIGH';blast_radius:'LOCAL'|'SYSTEM'|'EXTERNAL';reversibility:'REVERSIBLE'|'PARTIAL'|'IRREVERSIBLE';minimum_action:string}export function intentImpactGate(x:IntentImpact){if(!x.intent.trim()||!x.minimum_action.trim())return'REJECT';if(x.reversibility==='IRREVERSIBLE'||x.blast_radius==='EXTERNAL'||x.impact==='HIGH')return'HOLD';return'ALLOW'}
