export interface ContextPack{correlation_id:string;memory_refs:string[];evidence_refs:string[];failure_refs:string[];current_state_ref:string;created_at:string}export function validateContextPack(x:ContextPack){const s=(v:string)=>typeof v==='string'&&v.trim().length>0;return{ok:s(x.correlation_id)&&s(x.current_state_ref)&&s(x.created_at)&&Array.isArray(x.memory_refs)&&x.memory_refs.length>0&&Array.isArray(x.evidence_refs)&&x.evidence_refs.length>0&&Array.isArray(x.failure_refs)}}
