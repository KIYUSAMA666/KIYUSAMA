export type Containment='CONTINUE'|'DEGRADE'|'ISOLATE'|'STOP';export function containFailure(input:{contract_ok:boolean;side_effect:'NONE'|'REQUESTED'|'CONFIRMED'|'UNKNOWN';audit:'PASS'|'FAIL'|'AMBIGUOUS';memory:'VERIFIED'|'PARTIAL'|'INFERRED'|'UNKNOWN'|'CONTRADICTED'}):Containment{if(!input.contract_ok||input.audit==='FAIL'||input.memory==='CONTRADICTED')return'STOP';if(input.side_effect==='UNKNOWN'||input.audit==='AMBIGUOUS'||input.memory==='UNKNOWN')return'ISOLATE';if(input.memory==='PARTIAL'||input.memory==='INFERRED')return'DEGRADE';return'CONTINUE'}
