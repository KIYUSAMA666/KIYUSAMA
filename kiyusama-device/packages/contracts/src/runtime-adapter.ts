import type{RuntimeExecutor}from'./runtime-orchestrator.js';export interface CodexTaskBridge{submit:(input:{task_ref:string;correlation_id:string;authority_ref:string;evidence_refs:string[]})=>Promise<{branch_ref:string;evidence_refs:string[]}>}
export function codexTaskBridgeExecutor(bridge:CodexTaskBridge):RuntimeExecutor{return async chain=>{const d=chain.dispatch as Record<string,unknown>;const c=chain.claim as Record<string,unknown>;const r=await bridge.submit({task_ref:String(d.payload_ref),correlation_id:String(d.correlation_id),authority_ref:String(c.authority_ref),evidence_refs:Array.isArray(c.evidence_refs)?c.evidence_refs.map(String):[]});return{output_ref:r.branch_ref,evidence_refs:r.evidence_refs}}}
