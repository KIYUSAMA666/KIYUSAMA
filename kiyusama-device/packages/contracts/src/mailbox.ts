export interface MailboxMessage{message_id:string;correlation_id:string;from:string;to:string;payload_ref:string;evidence_refs:string[];status:'QUEUED'|'DELIVERED'|'REPLIED'|'FAILED';created_at:string}export function validateMailboxMessage(x:MailboxMessage){const s=(v:string)=>typeof v==='string'&&v.trim().length>0;return{ok:[x.message_id,x.correlation_id,x.from,x.to,x.payload_ref,x.created_at].every(s)&&Array.isArray(x.evidence_refs)&&['QUEUED','DELIVERED','REPLIED','FAILED'].includes(x.status)}}
