import type{RuntimeChain}from'./runtime-chain.js';import{orchestrateVerifiedChain,type RuntimeExecutor}from'./runtime-orchestrator.js';import{sideEffectFence}from'./side-effect-fence.js';export async function runOs2(chain:RuntimeChain,executor:RuntimeExecutor){const d=chain.dispatch as Record<string,unknown>;const fence=sideEffectFence({authority_ref:typeof d.authority_ref==='string'?d.authority_ref:null,side_effect_state:d.side_effect_state as 'NONE'|'REQUESTED'|'CONFIRMED'|'UNKNOWN',evidence_refs:Array.isArray(d.evidence_refs)?d.evidence_refs.map(String):[]});if(fence!=='ALLOW')return{ok:false,executed:false,reason:`SIDE_EFFECT_${fence}`,output_ref:null,evidence_refs:[]};return orchestrateVerifiedChain(chain,executor)}
