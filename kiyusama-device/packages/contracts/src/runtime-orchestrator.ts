import{verifyRuntimeChain,RuntimeChain}from'./runtime-chain.js';export type RuntimeExecutor=(chain:RuntimeChain)=>Promise<{output_ref:string;evidence_refs:string[]}>;export interface RuntimeOrchestrationResult{ok:boolean;executed:boolean;reason:string;output_ref:string|null;evidence_refs:string[]}
export async function orchestrateVerifiedChain(chain:RuntimeChain,executor:RuntimeExecutor):Promise<RuntimeOrchestrationResult>{const verified=verifyRuntimeChain(chain);if(!verified.ok)return{ok:false,executed:false,reason:'CONTRACT_CHAIN_REJECTED',output_ref:null,evidence_refs:[]};const result=await executor(chain);if(!result||typeof result.output_ref!=='string'||result.output_ref.trim()===''||!Array.isArray(result.evidence_refs)||result.evidence_refs.length===0)return{ok:false,executed:true,reason:'EXECUTOR_RESULT_UNVERIFIED',output_ref:null,evidence_refs:[]};return{ok:true,executed:true,reason:'VERIFIED_EXECUTION',output_ref:result.output_ref,evidence_refs:result.evidence_refs}}
