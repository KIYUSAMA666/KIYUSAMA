export interface CompletionEvidence{contracts_pass:boolean;integration_pass:boolean;kernel_boot_pass:boolean;memory_engine_pass:boolean;worker_fabric_pass:boolean;counter_lane_pass:boolean;tonton_boundary_pass:boolean;destructive_lock_intact:boolean;production_untouched:boolean}export function evaluateOs2Completion(e:CompletionEvidence){const missing=Object.entries(e).filter(([,v])=>v!==true).map(([k])=>k);return{complete:missing.length===0,missing}}
