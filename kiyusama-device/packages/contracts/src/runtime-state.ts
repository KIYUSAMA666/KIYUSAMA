export const RUNTIME_STATES=['RECEIVED','VALIDATED','CLAIMED','RUNNING','AMBIGUOUS','SUCCEEDED','FAILED','RECORDED']as const;export type RuntimeState=(typeof RUNTIME_STATES)[number];const allowed:Record<RuntimeState,RuntimeState[]>={RECEIVED:['VALIDATED','FAILED'],VALIDATED:['CLAIMED','FAILED'],CLAIMED:['RUNNING','FAILED'],RUNNING:['SUCCEEDED','FAILED','AMBIGUOUS'],AMBIGUOUS:['RECORDED'],SUCCEEDED:['RECORDED'],FAILED:['RECORDED'],RECORDED:[]};export function canTransitionRuntimeState(from:RuntimeState,to:RuntimeState){return allowed[from].includes(to)}
