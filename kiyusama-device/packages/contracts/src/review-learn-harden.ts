export interface HardeningRecord{failure_ref:string;root_cause:string;patch_ref:string;regression_test_ref:string;evidence_refs:string[]}export function validateHardeningRecord(x:HardeningRecord){const s=(v:string)=>typeof v==='string'&&v.trim().length>0;return{ok:[x.failure_ref,x.root_cause,x.patch_ref,x.regression_test_ref].every(s)&&Array.isArray(x.evidence_refs)&&x.evidence_refs.length>0}}
