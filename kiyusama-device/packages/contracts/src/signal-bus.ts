export interface SignalEnvelope{signal_id:string;correlation_id:string;source:string;target:string;payload_ref:string;evidence_refs:string[];received_at:string}export function validateSignalEnvelope(x:SignalEnvelope){const s=(v:string)=>typeof v==='string'&&v.trim().length>0;return{ok:[x.signal_id,x.correlation_id,x.source,x.target,x.payload_ref,x.received_at].every(s)&&Array.isArray(x.evidence_refs)}}
