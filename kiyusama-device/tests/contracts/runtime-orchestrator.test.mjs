import test from'node:test';import assert from'node:assert/strict';import{orchestrateVerifiedChain}from'../../dist/contracts/runtime-orchestrator.js';const at='2026-09-06T21:00:00+09:00';const chain={memory:{schema_version:'kiyusama-memory-continuation/2.0-draft1',memory_id:'m',subject:'s',state:'READY',previous_state:null,cause:'e',evidence_refs:[{ref:'r'}],version:1,freshness_at:at,source:'COMMON_MEMORY',verification_status:'VERIFIED',salience:'CORE',next_action:'dispatch',recorded_at:at},dispatch:{schema_version:'kiyusama-core-interface/2.0-draft1',event_id:'e',correlation_id:'c',source_plane:'MEMORY',target_plane:'WORKER',action:'DISPATCH',actor:'SORA',authority_ref:'a',evidence_refs:['r'],side_effect_state:'REQUESTED',payload_ref:'p',created_at:at},claim:{schema_version:'kiyusama-worker-claim/2.0-draft1',claim_id:'cl',task_id:'t',worker_id:'codex',lane:'IMPLEMENT',generation:1,authority_ref:'a',input_ref:'p',evidence_refs:['e'],state:'CLAIMED',claimed_at:at,released_at:null},worker_result:{schema_version:'kiyusama-worker-result/2.0-draft1',claim_id:'cl',task_id:'t',worker_id:'codex',generation:1,state:'SUCCEEDED',output_ref:'out',evidence_refs:['commit'],error:null,completed_at:at},audit:{schema_version:'kiyusama-counter-audit/2.0-draft1',audit_id:'au',subject_ref:'commit',producer:'CODEX',auditor:'KIRA',checks:['NODE','EDGE','LOOP','ABSENCE'],evidence_refs:['commit'],freshness_at:at,independent_recount:true,verdict:'PASS',findings:[],audited_at:at},delivery_event:{schema_version:'tonton-boundary/2.0-draft1',event_id:'de',correlation_id:'c',source:'WORKER',target:'MAILBOX',intent:'deliver',payload_ref:'out',evidence_refs:['commit'],created_at:at},delivery_result:{schema_version:'tonton-boundary-result/2.0-draft1',event_id:'de',correlation_id:'c',outcome:'DELIVERED',delivery_ref:'mailbox:1',evidence_refs:['receipt'],state_update_ref:'memory:v2',error:null,completed_at:at}};
test('valid chain reaches executor and returns evidenced output',async()=>{let calls=0;const r=await orchestrateVerifiedChain(chain,async()=>{calls++;return{output_ref:'branch:2',evidence_refs:['commit:2']}});assert.equal(calls,1);assert.equal(r.ok,true)});test('invalid authority fails before executor side effect',async()=>{let calls=0;const bad={...chain,dispatch:{...chain.dispatch,authority_ref:null}};const r=await orchestrateVerifiedChain(bad,async()=>{calls++;return{output_ref:'x',evidence_refs:['x']}});assert.equal(calls,0);assert.equal(r.executed,false);assert.equal(r.ok,false)});
