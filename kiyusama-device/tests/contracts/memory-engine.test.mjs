import test from'node:test';import assert from'node:assert/strict';import{continueMemory}from'../../dist/contracts/memory-engine.js';const a='2026-09-06T21:10:00+09:00',b='2026-09-06T21:11:00+09:00';const record={schema_version:'kiyusama-memory-continuation/2.0-draft1',memory_id:'m',subject:'OS2',state:'BUILDING',previous_state:null,cause:'start',evidence_refs:[{ref:'commit:1'}],version:1,freshness_at:a,source:'SORA',verification_status:'VERIFIED',salience:'CORE',next_action:'continue',recorded_at:a};test('writes causal diff into next memory version',()=>{const n=continueMemory({record,after_state:'TESTING',cause:'implementation complete',evidence_refs:[{ref:'commit:2'}],actor:'SORA',timestamp:b,next_action:'run tests'});assert.equal(n.version,2);assert.equal(n.previous_state,'BUILDING');assert.equal(n.state,'TESTING');assert.equal(n.evidence_refs[0].ref,'commit:2')});test('cannot promote VERIFIED without evidence',()=>assert.throws(()=>continueMemory({record,after_state:'DONE',cause:'claim',evidence_refs:[],actor:'SORA',timestamp:b,next_action:'ship'})));
