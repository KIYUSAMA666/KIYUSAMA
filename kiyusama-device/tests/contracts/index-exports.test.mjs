import test from'node:test';import assert from'node:assert/strict';import*as contracts from'../../dist/contracts/index.js';test('index exports executable OS2 contracts',()=>{for(const name of['validateMemoryContinuationRecord','validateCoreInterfaceEnvelope','validateWorkerClaim','validateWorkerResult','validateCounterAudit','validateTontonBoundaryEvent','validateTontonBoundaryResult','verifyRuntimeChain'])assert.equal(typeof contracts[name],'function',`${name} missing`)});