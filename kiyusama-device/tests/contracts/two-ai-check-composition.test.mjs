import test from'node:test';import assert from'node:assert/strict';import{requiredVerificationDepth}from'../../dist/contracts/verification-depth.js';import{validateCounterAudit}from'../../dist/contracts/counter-lane.js';import{independentRecount}from'../../dist/contracts/independent-recount.js';

test('TWO_AI_CHECK_WHERE_APPROPRIATE composes risk depth with an independent auditor',()=>{assert.equal(requiredVerificationDepth({risk:'HIGH',side_effect:'NONE'}),'INDEPENDENT_RECOUNT');const audit={schema_version:'kiyusama-counter-audit/2.0-draft1',audit_id:'audit-1',subject_ref:'subject-1',producer:'SORA',auditor:'KIRA',checks:['NODE'],evidence_refs:['evidence-1'],freshness_at:'2026-09-06T11:22:12Z',independent_recount:true,verdict:'PASS',findings:[],audited_at:'2026-09-06T11:22:12Z'};assert.equal(validateCounterAudit(audit).ok,true);assert.deepEqual(independentRecount({producer_count:1,auditor_count:1,producer:'SORA',auditor:'KIRA',evidence_refs:['evidence-1']}),{ok:true,reason:'MATCH'})});

test('same producer cannot satisfy an independent two-AI check',()=>{const audit={schema_version:'kiyusama-counter-audit/2.0-draft1',audit_id:'audit-2',subject_ref:'subject-2',producer:'SORA',auditor:'SORA',checks:['NODE'],evidence_refs:['evidence-2'],freshness_at:'2026-09-06T11:22:12Z',independent_recount:true,verdict:'PASS',findings:[],audited_at:'2026-09-06T11:22:12Z'};assert.equal(validateCounterAudit(audit).ok,false);assert.deepEqual(independentRecount({producer_count:1,auditor_count:1,producer:'SORA',auditor:'SORA',evidence_refs:['evidence-2']}),{ok:false,reason:'SELF_RECOUNT'})});

test('ordinary low-risk work does not force the independent recount lane',()=>{assert.equal(requiredVerificationDepth({risk:'LOW',side_effect:'NONE'}),'BASIC')});
