import test from'node:test';import assert from'node:assert/strict';import{authenticityGate}from'../../dist/contracts/authenticity.js';test('all authenticity conditions required',()=>assert.equal(authenticityGate({signature_valid:true,source_trusted:true,fresh:true}),'VERIFIED'));test('stale evidence is unverified',()=>assert.equal(authenticityGate({signature_valid:true,source_trusted:true,fresh:false}),'UNVERIFIED'));