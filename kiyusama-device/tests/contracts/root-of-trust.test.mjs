import test from'node:test';import assert from'node:assert/strict';import{isTrustedAnchor}from'../../dist/contracts/root-of-trust.js';test('trusted anchor accepted',()=>assert.equal(isTrustedAnchor({anchor_id:'github-main',kind:'REPOSITORY',ref:'KIYUSAMA666/KIYUSAMA',status:'TRUSTED'}),true));test('revoked anchor rejected',()=>assert.equal(isTrustedAnchor({anchor_id:'x',kind:'PROVIDER',ref:'x',status:'REVOKED'}),false));