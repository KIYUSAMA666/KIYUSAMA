import test from'node:test';import assert from'node:assert/strict';import{requiredVerificationDepth}from'../../dist/contracts/verification-depth.js';test('high risk requires independent recount',()=>assert.equal(requiredVerificationDepth({risk:'HIGH',side_effect:'NONE'}),'INDEPENDENT_RECOUNT'));test('unknown side effect requires independent recount',()=>assert.equal(requiredVerificationDepth({risk:'LOW',side_effect:'UNKNOWN'}),'INDEPENDENT_RECOUNT'));test('low no-side-effect can use basic verification',()=>assert.equal(requiredVerificationDepth({risk:'LOW',side_effect:'NONE'}),'BASIC'));
