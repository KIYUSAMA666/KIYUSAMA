import test from'node:test';import assert from'node:assert/strict';import{evaluateOs2Completion}from'../../dist/contracts/completion-gate.js';const all={contracts_pass:true,integration_pass:true,kernel_boot_pass:true,memory_engine_pass:true,worker_fabric_pass:true,counter_lane_pass:true,tonton_boundary_pass:true,destructive_lock_intact:true,production_untouched:true};test('completion requires every executable gate',()=>assert.equal(evaluateOs2Completion(all).complete,true));test('missing test evidence prevents completion claim',()=>assert.equal(evaluateOs2Completion({...all,contracts_pass:false}).complete,false));
