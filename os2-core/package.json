{
  "name": "kiyusama-os2-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "rm -rf dist && tsc -p tsconfig.json && mkdir -p dist/sql && cp sql/*.sql dist/sql/ && node --test dist/tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.2"
  }
}
